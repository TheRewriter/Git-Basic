/*
    index.html 에서 사용되는 js
*/
function login(username, password, flag) {
    //login 처리
    if(usrname ~== null && password !== null && flag){
        //username값과 password값을 flag 체크
    }
    return true;
};

function logout(username) {
    //logout 처리
};